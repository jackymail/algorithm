/*
 *Given an array nums of integers and int k.
 *partition the array,(i.e move the elements in "nums")
 *such that:
 *All elements < k are move to the left
 *All elements >= k are moved to the right
 *return the partitioning index,i.e the first index i
 *nums[i] > k
 *for example if nums = [3,2,2,1] and k=2. the valid answer is 1
 */


